# Trabalho 6 (T6) – Geração de Código III (Peso: 40% da M3)
### Apresentação em: 10/12/2025

### Requisitos

Realizar a geração de código do programa usando o conjunto de instruções do
processador BIP, As seguintes instruções devem ter seu código gerado:

- [ ] (1 ponto) Chamada de sub-rotinas (gestão de rótulos e calls)
- [ ] (3 pontos) Passagem de parâmetros (por cópia)
- [ ] (2 pontos) Retorno de funções (permitir em expressões e atribuições)
- [ ] (3 pontos) Compatibilidade entre parâmetros (tipo, ordem e quantidade)
- [ ] (1 pontos) Gerar mensagens de aviso e erro significativas para o usuário
(ex.: (i) a função “teste” esperava 2 parâmetros e foram passado X
parâmetros; (ii) a rotina “coisa” não existe,...)

# 🔗 Anotações sobre o trabalho: 

### Observações:

- Não precisa ter chamada de função dentro de função 

- Sobre parâmetros:
  - Não precisa permitir parâmetro opcional
  - Deve verificar compatibilidade de tipos na passagem de parâmetros
  - Deve verificar quantidade de parâmetros na chamada, se bate com a declaração da função
  - Para saber para qual parametro deve ser copiado o valor, deve-se seguir a ordem dos parametros da função, através de um contador de parametros. (exemplos nos slides do professor)
- Sobre função: 
  - Deve ser possível retornar valores de funções `a = soma(1, 2)`
  - O valor de retorno vai ser salvo no ACC
  - Em caso de atribuição o valor do ACC vai ser copiado para a variável que chamou a função
  - Deve verificar compatibilidade de tipos no retorno com o tipo da função
  - pode ter ou não return
    - return sempre vai ser 0 (no BIP), caso precise retornar algo, ele vai estar sendo salvo no ACC.
    - deve verificar compatibilidade de tipos no return com o tipo da função
  - pode ter operações aritméticas envolvendo função
  - ex: `a = T(1, 2) + vet[i]`
  - o ponto de entrada da rotina é o **void T** que vai ser definida por um rótulo no código BIP



```
void T (int a, int b){
  ...resto do codigo da função
}

void main(){
  int a;
  T(a, 5);
}
```

#### Para diferenciar as variáveis globais das locais, as globais devem ser declaradas na seção .data, as de funções devem ser declaradas utilizando um prefixo no nome da variavel de parametro ou local da função, por exemplo:

```
.data
  a_T: 0
  b_T: 0
  a_main: 0
.text
 JMP main
F_T:
 // codigo da função T
 RETURN 0
main:
  LD a_main
  STO a_T
  LDI 5
  STO b_T
  CALL F_T
```


